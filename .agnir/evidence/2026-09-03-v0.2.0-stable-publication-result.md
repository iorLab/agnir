# v0.2.0 Stable Publication Result — 2026-09-03

## Publication target

- tag: `v0.2.0`;
- exact tag target: `fc84095ed5d500be9e1b43a4af0e93356571bbd4`;
- tag object type: direct commit ref;
- stable package provenance baseline: `f59a83754346982170142a355a01c94050ddf3a5`;
- publication/conformance workflow: `33711982062`.

## Workflow result

Run `33711982062` executed the complete repository conformance job on exact publication revision `fc84095...` before the dependent stable publication job could receive `contents:write`.

The conformance job passed Core `0.2` self-host, Core/profile `0.1` regression, VCS and non-VCS lineage pressure, repository-filesystem `0.2`, selector/binding, semantic and concrete `0.1` → `0.2` migration, fresh Core `0.2` installation, exact published-`v0.1.1` migration/fresh resume, stable `v0.2.0` package gates and the full suite.

The stable publication job then succeeded. The RC publication job was skipped.

## External verification

Independent GitHub reads after the workflow completed confirmed:

- `refs/tags/v0.2.0` resolves to `fc84095ed5d500be9e1b43a4af0e93356571bbd4`;
- GitHub Release id `381710267` has tag `v0.2.0`;
- `draft=false`;
- `prerelease=false`;
- `target_commitish=fc84095ed5d500be9e1b43a4af0e93356571bbd4`;
- `releases/latest` resolves to the same Release and tag `v0.2.0`.

The previously published RC remains immutable at `v0.2.0-rc.1` -> `50a8cd565954e7e8055b8b628e2d620ac7357bab`.

## Provenance

`extensions.agnir/operations.applied_revision` records verified package baseline `f59a837...`, not the final tag target. This avoids self-referential SHA provenance while preserving an immutable receipt for the package that was applied before the final publication transition.

## Next boundary

Stable publication on the release lineage does not copy release continuity into authoritative main. Main reconciliation must preserve main lineage identity/binding, stage without main advancement, reconcile target continuity against the actual integrated Project result and release Evidence, validate the exact candidate, stale-check source/target refs, then publish main coherently once.
