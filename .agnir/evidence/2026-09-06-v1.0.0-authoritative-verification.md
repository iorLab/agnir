# v1.0.0 authoritative main verification

Date: 2026-09-06

## Verdict

**AUTHORITATIVE SOURCE VERIFIED; PUBLICATION NOT ARMED.**

## Exact authoritative source

- branch: `main`
- exact revision: `ab5dcc3341d39631e843499632739864a90bba14`
- tree: `9b4bfb2c9a5eec48f062add710759a6163d5fa04`
- Project identity: `urn:agnir:project:agnir-core`
- logical lineage: `urn:agnir:lineage:authoritative`
- selector: `refs/heads/main`
- repository version: `1.0.0`
- Core/profile: `1.0` / `repository-filesystem/1.0`

## Verification

Workflow `34030974021`, repository job `101480276759`, ran on the exact authoritative revision and succeeded.

Successful gates included:

- Core/profile 1.0 self-host cold start;
- historical Core/profile 0.1 compatibility regression;
- Core/profile 0.2 VCS and non-VCS continuity;
- repository-filesystem 0.2 and 1.0 discovery;
- Core 1.0 stability semantics;
- explicit 0.2 -> 1.0 promotion;
- VCS lineage binding;
- Core/profile 0.1 -> 0.2 migration;
- fresh Core 0.2 install and published v0.1.1 migration;
- explicit stable package gates on `main`;
- complete `test_*.py` suite.

The stable publication job was skipped because the commit message was not the exact arm message, which is the required behavior at this checkpoint.

The same target content had already passed validation run `34030934536` before main advancement. Main was re-read immediately before advancement and still matched captured base `11148c3063e63dd1ea7450b9d538ac1eccec5639`.

## Accepted dependencies

- stable staging source `65b5484b62bbd413d0984d5c952bc0a653da1964`, run `34030660332`;
- stable staging checkpoint `5f88a9c8bcc67753012f8bcae533241482dc1a7d`, run `34030753962`;
- immutable accepted RC `v1.0.0-rc.1` -> `092945289f1a0a9803e4fe0583104aa380ceaadc`;
- RC Release id `383536840`;
- RC workflow `34026167762` attempts 1 and 2 success;
- RC acceptance checkpoint `afc07d062b957e8dbfe3f859834c787e64aa52be`.

## Next boundary

A continuity-only checkpoint now records this verification. Once that checkpoint itself passes exact-main CI, the stable publication arm is authorized as a separate main commit whose message is exactly:

`release: publish v1.0.0 stable`

That arm commit must not contain unrelated semantic changes.
