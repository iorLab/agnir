# Agnir v1.0.2 publication candidate

Date: 2026-09-18

## Authorization

The Principal explicitly authorized: **publish `v1.0.2`**.

## Scope

`1.0.2` is a PATCH-only distribution/onboarding release for the already accepted ChatGPT Project locator simplification.

Accepted persistent handoff:

```text
Canonical Project: <owner/repository> (<ref>)
At the first substantive turn of every new conversation, open it, read root AGENTS.md, and follow it before doing Project work.
```

The release does not change Core `1.0`, `repository-filesystem/1.0`, Project identity, Continuity Lineage, checkpoint semantics, or historical compatibility lines.

## Accepted implementation input

- implementation PR: `#58`;
- implementation merge: `2c62d026c48405c3e299f0f57d6a674b3f6f9e1c`;
- implementation post-merge conformance: `35353464865` — success;
- Principal acceptance/checkpoint merge: `4fde39128e5edf3fba3d7906a279918b1c38b7c2`;
- acceptance post-merge conformance: `35353700740` — success;
- acceptance repository job: `105627739027` — success.

## Candidate packaging

Staging ref: `release/v1.0.2`.

The candidate changes:

- repository `VERSION` to `1.0.2` while keeping Core/profile `1.0`;
- records `1.0.2` candidate metadata while `latest_stable` remains `v1.0.1` pre-publication;
- defines the `1.0.2` locator-only upgrade behavior in `SKILL.md`;
- extends 1.0.x self-host/stable package conformance to `1.0.2`;
- adds a publication job that can run only on authoritative `main` when the head commit message is exactly `release: publish v1.0.2 stable`;
- requires the publication job to verify immutable `v1.0.1`, `v1.0.0`, and accepted `v1.0.0-rc.1` tag receipts before and after creating `v1.0.2`.

## Publication boundary

Candidate implementation or merge does not publish. Publication is accepted only after:

1. candidate conformance succeeds;
2. authoritative-main reconciliation and post-merge conformance succeed;
3. the exact publication-arm commit triggers the `v1.0.2` publication job;
4. `refs/tags/v1.0.2` resolves to the exact arm commit;
5. the GitHub Release is non-draft/non-prerelease;
6. `releases/latest == v1.0.2`;
7. `v1.0.1`, `v1.0.0`, and `v1.0.0-rc.1` remain unchanged.

## Pre-checkpoint candidate receipts

- exact candidate head before this Evidence reconciliation: `907479d79921a8733052adb3384a58270a79bee1`;
- workflow: `35354730577` — success;
- repository job: `105631181979` — success;
- stable package gates: success;
- full conformance suite: success;
- publication jobs: skipped, as required on the staging branch.

This Evidence update becomes the final staging checkpoint and therefore advances the branch once more. The resulting checkpointed head requires one final branch conformance run before PR merge.
