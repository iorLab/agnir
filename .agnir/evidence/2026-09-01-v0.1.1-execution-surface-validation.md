# v0.1.1 execution-surface validation — 2026-09-01

## Purpose

This Evidence records the real execution-surface regression gate required before preparing Agnir repository release `0.1.1`.

## Regression target

- execution surface: ChatGPT web Project
- ChatGPT Project: `skills-hub`
- canonical repository: `mattamior/skills-hub`
- authoritative ref: `main`
- Agnir compatibility: Core `0.1`, `repository-filesystem/0.1`

The earlier initialization had established the repository-owned activation route but had not supplied the persistent ChatGPT Project Instructions needed for a genuinely fresh conversation to locate the Project. That gap motivated the execution-surface activation handoff repair.

## Configuration applied

The Principal appended/merged the locator-only bootstrap produced by the repaired Agnir Skill into the ChatGPT Project's persistent Instructions. The bootstrap identifies `mattamior/skills-hub` / `main`, directs a fresh conversation to root `AGENTS.md`, and defers durable Project truth to the repository's `AGNIR.yaml` and declared memory. It does not copy Current State, Next Actions, Decisions, Evidence, or the full Agnir procedure into ChatGPT settings.

## Fresh-context result

A genuinely new conversation was opened inside that ChatGPT Project. The first substantive user request was an ordinary Project request — to report the Project's current state and next step — without mentioning Agnir or supplying the repository address again.

The fresh conversation immediately stated that it would use the repository-managed Agnir durable state, identified `mattamior/skills-hub`, and began fetching root `AGENTS.md`, `AGNIR.yaml`, and the declared continuity files. The Principal supplied a screenshot of this behavior and explicitly reported the regression test as **passed**.

Observed activation path:

```text
ChatGPT Project persistent Instructions
→ mattamior/skills-hub
→ root AGENTS.md
→ README.md / Agnir Project Instructions
→ AGNIR.yaml
→ declared durable continuity
```

The fresh conversation did not require the original initialization conversation, a repeated Agnir bootstrap prompt, or a second copy of Project state in ChatGPT settings.

## Result

- repository activation repair: **passed**
- ChatGPT execution-surface persistent locator: **configured**
- genuinely fresh conversation activation: **passed**
- real `skills-hub` regression gate for `v0.1.1`: **passed**

This result closes the execution-surface validation gate identified in the previous repair Evidence. Agnir `0.1.1` may now proceed to release-candidate preparation and exact-candidate conformance. The immutable `v0.1.1` tag/release must still target only a revision that passes the full publication workflow.
