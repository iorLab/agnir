# v1.0.0 Stable Publication Arm

Status: armed; publication outcome pending at commit creation.

## Preconditions

- authoritative target revision `ab5dcc3341d39631e843499632739864a90bba14` passed main workflow `34030974021`, repository job `101480276759`;
- publication-precondition checkpoint `b99ea0d37cca852df023ef9071d4102c0376f0fe` passed main workflow `34038863673`, repository job `101501837860`;
- both runs passed self-host, stable package gates, and the full conformance suite;
- accepted immutable RC remains `v1.0.0-rc.1` at `092945289f1a0a9803e4fe0583104aa380ceaadc`, Release id `383536840`;
- stable publication was skipped before this arm transition.

## Arm rule

The sole publication trigger is a push to authoritative `main` whose head commit message is exactly:

`release: publish v1.0.0 stable`

This transition records publication authorization/status only. It does not alter Core/profile semantics, schemas, resolvers, promotion behavior, compatibility support, package documentation, or release content.

## Acceptance rule

Publication is accepted only after the same exact arm SHA has both:

1. successful `repository-filesystem` conformance; and
2. successful `Publish v1.0.0 stable release` job.

Independent post-publication verification must then prove `v1.0.0` tag == arm SHA, Release is non-draft/non-prerelease, `releases/latest` == `v1.0.0`, and accepted RC identity remains unchanged.
