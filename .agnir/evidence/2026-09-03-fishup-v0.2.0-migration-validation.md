# FishUp published v0.1.1 -> v0.2.0 real migration-line validation

Date: 2026-09-03

## Scope

The Principal explicitly authorized a real Agnir migration validation on `iorLab/fishup`. FishUp is a materially different downstream Project: a Vite/TypeScript/three.js/Rapier game with Cloudflare Workers + D1 deployment and existing Project-owned Agnir continuity.

This Evidence records a validated migration **line**. It does not claim FishUp authoritative `main` was migrated, because every main push triggers remote D1 migration attempts plus Cloudflare Workers deployment and that production side effect was deliberately kept outside the validation authorization.

## Captured source

- downstream repository: `iorLab/fishup`;
- Project identity: `urn:agnir:project:fishup`;
- captured authoritative main: `a57b5ec7b679874af08da094fef3e8e7bdee90e3`;
- source Core/profile: `0.1` / `repository-filesystem/0.1`;
- source operational release: `v0.1.1`;
- source applied revision: `e9712357ab590e5c1e5357b3cf3219d07d789aff`;
- durable locators: `.agnir/state.md`, `.agnir/next-actions.md`, `.agnir/decisions.md`, `.agnir/evidence/`.

## Validated target line

- migration branch: `migration/agnir-v0.2.0-stable`;
- first migration commit: `c9673cf3c20a0fa92db54c9d54413efe7fefc7ee`;
- first migration tree: `b2bd12f9f64e6f26abfb804b65692fa45217ca97`;
- verified evidence checkpoint/head: `bea8c4e6e52347e1a0164596a5a9132b17de9631`;
- checkpoint tree: `1bd27517a2c663e5303bd22190129f65806426e6`;
- target Core/profile: `0.2` / `repository-filesystem/0.2`;
- published Agnir release: `v0.2.0` -> `fc84095ed5d500be9e1b43a4af0e93356571bbd4`;
- logical lineage: `urn:fishup:lineage:agnir-v0.2.0-stable-migration`;
- selector: `refs/heads/migration/agnir-v0.2.0-stable`;
- validation Draft PR: FishUp #1, explicitly `DO NOT MERGE`.

## Verification receipts

Run `33737783270` completed successfully on the first migration commit. Run `33737919224` completed successfully on the exact evidence checkpoint.

The branch-only workflow checked out the immutable published `iorLab/agnir@v0.2.0` tag and directly exercised its repository/filesystem 0.2 reference migration/resolver against a detached copy of the actual captured FishUp baseline.

The validation proved:

- unauthorized Core/profile `0.1 -> 0.2` migration is rejected;
- authorized migration succeeds;
- Project identity is preserved;
- State / Next Actions / Decisions survive byte-exact;
- identical migration re-application is a no-op;
- the actual FishUp migration branch fresh-resolves under the published repository-filesystem 0.2 resolver with the expected Project identity and logical lineage;
- lineage identity is separate from VCS selector and commit receipt;
- operational provenance points to published Agnir `v0.2.0`;
- `AGENTS.md` and `README.md` remain byte-exact;
- all FishUp product source/config/assets remain unchanged;
- `pnpm install --frozen-lockfile`, `pnpm typecheck`, and `pnpm build` succeed;
- compare against captured main shows exactly three migration-only changed paths: `AGNIR.yaml`, migration Evidence, and branch-only validation workflow;
- no Cloudflare deployment occurred from the migration validation path.

No Agnir Core/profile 0.2 defect was observed.

## v1 interpretation

FishUp is now a second materially different external real Project with direct Core/profile 0.2 migration behavior evidence, in addition to Svif. This strengthens the real-Project gate but does not by itself close it: FishUp authoritative main acceptance remains pending because of the production-deploy boundary, and at least one additional materially different real Project is still desired under the current stricter readiness map. `iorLab/VocaPort` remains the preferred fresh-install candidate.

This Evidence must not be cited as proof that FishUp production/main is already on Agnir 0.2.
