# v0.2.0 Stable Package Baseline and Publication Candidate — 2026-09-03

## Purpose

Record the immutable package baseline used to construct Agnir repository release `v0.2.0` and the boundary between package provenance and the future stable tag target.

## Inputs

- authoritative main checkpoint: `1af33e0cc470107aadaeb5d4d2f0f4570d81ee1d`;
- accepted immutable RC tag: `v0.2.0-rc.1` -> `50a8cd565954e7e8055b8b628e2d620ac7357bab`;
- accepted RC Release id: `381532232`;
- stable release carrier: `refs/heads/release/v0.2.0`;
- stable logical lineage: `urn:agnir:lineage:v0.2.0`.

## Stable package baseline

Coherent stable package baseline:

- commit: `f59a83754346982170142a355a01c94050ddf3a5`;
- tree: `2605d39fd355cd98939ac2862dcf56c2764ce29c`;
- workflow run: `33711830312`;
- result: success.

The baseline promotes repository SemVer to `0.2.0`, promotes Core/profile `0.2` documents from RC-candidate status to stable normative status, synchronizes bilingual README/release/versioning/repository-tree documentation, adds generic Core `0.2` self-host pressure and stable release gates, and preserves Core/profile `0.1` compatibility and migration surfaces.

The baseline run passed Core `0.2` self-host, Core/profile `0.1` regression, non-VCS and VCS lineage pressure, repository-filesystem `0.2`, lineage binding, semantic and concrete `0.1` → `0.2` migration, fresh install plus exact published-`v0.1.1` migration/fresh resume, stable package gates, and the full conformance suite.

The baseline commit message did not match either publication trigger. Both stable and RC publication jobs were skipped. Therefore this successful package run created no new release tag and did not alter the existing RC tag.

## Provenance rule

The stable publication candidate records:

- `extensions.agnir/operations.release: "0.2.0"`;
- `extensions.agnir/operations.applied_revision: "f59a83754346982170142a355a01c94050ddf3a5"`.

This is deliberate. The final publication commit cannot embed its own unknown future SHA into content that determines that SHA. The already verified package baseline is the immutable operational provenance; the later exact publication commit is the tag target and publication receipt.

## Publication gate

Stable publication remains incomplete until the exact publication candidate:

1. passes the complete conformance job;
2. creates/verifies lightweight tag `v0.2.0` only if absent or already pointing to the same exact SHA;
3. creates/verifies a GitHub Release with `draft=false` and `prerelease=false`;
4. verifies GitHub `releases/latest` resolves to `v0.2.0`.

A conflicting existing tag is a hard failure. Published tags are immutable and must never be moved as recovery behavior.

## Main boundary

Publication on `release/v0.2.0` does not automatically make release-line continuity authoritative-main truth. After publication, accepted Project/package changes and publication facts must be staged and reconciled back into main while preserving main logical lineage `urn:agnir:lineage:authoritative` and selector `refs/heads/main`.
