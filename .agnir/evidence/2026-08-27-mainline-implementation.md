# Evidence — Agnir main-line implementation start

Date: 2026-08-27

This evidence record marks the transition from architecture-only drafts to the first executable Agnir `0.1` main-line structure.

Implemented:

- top-level `AGNIR.yaml` repository/filesystem discovery anchor;
- authoritative `.agnir/` state, next actions, decisions, and evidence locations;
- normative working Core and Discovery documents;
- normative repository/filesystem profile;
- JSON Schema for the reference `AGNIR.yaml` serialization;
- executable structural cold-start conformance check;
- ChatGPT bootstrap compatibility shim redirected to the Agnir state.

Predecessor PPMP v2.0.0 remains preserved on `legacy/ppmp-v2.0.0` and is not relabeled as Agnir conformance.
