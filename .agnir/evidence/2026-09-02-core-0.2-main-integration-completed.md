# Core 0.2 authoritative-main integration completed — 2026-09-02

## Integrated revision

Authoritative `main` advanced from `1bbdb5b258645ec7c5e0109c9b17dbaac004e625` directly to reconciled two-parent revision `a32c9143687b72426617ddd701b90ffd237a111c`.

Parents:

1. previous authoritative `main`: `1bbdb5b258645ec7c5e0109c9b17dbaac004e625`;
2. Core `0.2` source checkpoint: `68cc443d6c44929f1b71d9d534e9b0f73f9745bf`.

Candidate/main tree: `759766c34e0f39f0c8d51bea1af22d7d41ad591c`.

The tree used the complete Core `0.2` Project result plus target-owned reconciled `AGNIR.yaml`, Current State, Next Actions, Decisions, and safe-integration evidence. Published `v0.1.1` truth and stable Core/profile `0.1` self-hosting were preserved; Core/profile `0.2` remained pre-RC.

## Pre-publication validation

Temporary integration ref: `integration/core-0.2-main-candidate`.

Draft validation PR `#6` generated GitHub test merge `270265a4d76b92553176a5a0b281f9202b7644fd`. Its tree SHA was exactly `759766c34e0f39f0c8d51bea1af22d7d41ad591c`, identical to candidate `a32c914...`.

Candidate-tree run `33653019074` passed:

- stable self-hosting cold-start conformance;
- experimental VCS branch continuity;
- Core `0.2` non-VCS parallel continuity;
- Core `0.2` VCS mapping;
- repository-filesystem `0.2` discovery;
- VCS lineage binding;
- Core `0.1`→`0.2` migration semantics;
- concrete repository-filesystem `0.1`→`0.2` migration;
- full conformance suite.

Before publication, fresh ref reads still showed target `main` at `1bbdb5b...` and source `feature/core-0.2-lineage` at `68cc443d...`. The validated candidate was therefore not stale.

## Publication

`main` was advanced once, non-forced, directly to exact candidate `a32c9143687b72426617ddd701b90ffd237a111c`.

There was no ordinary GitHub merge-first/follow-up-continuity-repair path and no intermediate target revision carrying feature-local continuity as main truth.

Fresh ref verification immediately afterward resolved `main` to `a32c914...`.

## Authoritative-main verification

Main push run `33653087179` completed successfully on exact `main@a32c9143687b72426617ddd701b90ffd237a111c` and passed the same stable, experimental, migration, and full-suite gates.

Fresh `AGNIR.yaml` on authoritative main still declares:

- Core `0.1`;
- `repository-filesystem/0.1`;
- Project `urn:agnir:project:agnir-core`;
- authoritative ref `main`;
- operational release `0.1.1` anchored to `e9712357ab590e5c1e5357b3cf3219d07d789aff`;
- Core `0.2` experiment status `real-consumer-validated` / `pre-rc`.

Thus integrating Core `0.2` implementation/evidence did not silently claim that Agnir itself had already migrated or that `v0.2.0` had been published.

## PR ancestry result

- PR `#4` was automatically marked merged because its head is ancestry of the exact main revision.
- Validation PR `#6` was automatically marked merged at exact candidate `a32c914...`.
- Stacked PR `#5` remained open because its base is `feature/multibranch-continuity`, even though its head `68cc443d...` is the second parent of main integration. A completion comment documented the exact integration receipts, then PR `#5` was closed without creating a second merge path.

## Conclusion

The pre-RC safe-main gate is satisfied:

1. target continuity was reconciled before target advancement;
2. exact candidate tree passed conformance while target remained unchanged;
3. source/target freshness was rechecked before publication;
4. target advanced once directly to the reconciled candidate;
5. authoritative main passed conformance afterward;
6. stable release/Core-profile truth remained intact;
7. stacked PR history was resolved without a duplicate merge path.

The next release boundary is `v0.2.0-rc.1` preparation and real install/migration/resume validation.
