# v0.2.0 Stable-to-Main Reconciliation — 2026-09-03

## Purpose

Safely accept the published stable `v0.2.0` Project/package result into authoritative `main` without copying release-line continuity as target truth or advancing main before reconciliation is complete.

## Captured target

- ref: `refs/heads/main`;
- revision: `1af33e0cc470107aadaeb5d4d2f0f4570d81ee1d`;
- Project: `urn:agnir:project:agnir-core`;
- logical lineage: `urn:agnir:lineage:authoritative`;
- selector: `refs/heads/main`.

## Captured source

- ref: `refs/heads/release/v0.2.0`;
- post-publication checkpoint: `2eb083d2aaa2a0869b2baf9ba46d012913317102`;
- source logical lineage: `urn:agnir:lineage:v0.2.0`;
- source selector: `refs/heads/release/v0.2.0`;
- source checkpoint CI: `33712203988` success.

Published stable receipts carried by the source include:

- `v0.2.0` -> `fc84095ed5d500be9e1b43a4af0e93356571bbd4`;
- Release id `381710267`, `draft=false`, `prerelease=false`;
- publication/conformance run `33711982062` success;
- `releases/latest` -> `v0.2.0`;
- package provenance baseline `f59a83754346982170142a355a01c94050ddf3a5`, run `33711830312` success.

## Reconciliation

Accepted Project/package inputs from the release lineage:

- repository `VERSION=0.2.0`;
- stable Core `0.2` normative contract;
- stable `repository-filesystem/0.2` normative profile;
- stable bilingual README, RELEASE, VERSIONING, milestone and repository-tree documentation;
- generic Core `0.2` self-host checker and stable release gate;
- release publication Evidence.

Target truth retained/reconstructed for main:

- Project identity remains `urn:agnir:project:agnir-core`;
- logical lineage remains `urn:agnir:lineage:authoritative`;
- selector remains `refs/heads/main`;
- release-line lineage/selector and release-line State/Next Actions are not target truth;
- `extensions.agnir/operations` records stable package `0.2.0` applied from verified baseline `f59a837...`;
- stable publication receipts are accepted as observed Project/release facts.

## Publication invariant

The integration candidate is constructed before main advancement. Candidate validation must complete on the exact tree while main remains at the captured target. Immediately before publication, both target and source refs must be re-read. Any source/target change invalidates the candidate.

After successful candidate validation and fresh stale-check, main may advance exactly once to the reconciled two-parent revision. Ordinary server-side merge is not the publication primitive.

## Post-publication requirement

Fresh main discovery must resolve Core/profile `0.2`, the same Project identity, main logical lineage `urn:agnir:lineage:authoritative`, and selector `refs/heads/main`. Main push CI must pass the full conformance suite. A later checkpoint records those new receipts without altering the immutable stable tag.
