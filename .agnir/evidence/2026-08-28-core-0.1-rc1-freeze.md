# Agnir Core 0.1 RC1 freeze evidence

Date: 2026-08-28

## Frozen version layers

- Core compatibility: `agnir.version: "0.1"`.
- Repository/filesystem profile compatibility: `repository-filesystem/0.1`.
- Repository release version: `0.1.0-rc.1`.
- Intended first stable repository release for this Core line: `0.1.0`.

The Core compatibility line and repository SemVer are intentionally separate. Consumers bind to Core `"0.1"`; patch/RC repository releases do not create new Core compatibility lines.

## Compatibility rule

Within repository release family `0.1.x`:

- prose may be clarified;
- implementations may be fixed;
- conforming fixtures/profiles may be added;
- conformance may be strengthened;

but existing Core `0.1` field meaning, required durable-memory semantics, discovery failure semantics, and compatibility claims MUST NOT be redefined.

A deliberate breaking Core semantic change requires a new compatibility line such as `"0.2"` and a new repository minor release line such as `0.2.0`.

## Predecessor / migration evidence

Real non-fixture migration pressure:

- `iorLab/svif@legacy/zerolocal-v0.1` at `8ccbb1d30520ca3d0b8b9f2cfe2963d35a853cf6`;
- classified correctly as v1/RPM-era predecessor evidence, not PPMP v2;
- material durable-knowledge audit detected and repaired the lost Svif `installable-plugin` product target.

Exact PPMP v2 source:

- canonical predecessor branch `legacy/ppmp-v2.0.0`;
- boundary commit `3bd3938ea00276eb51ca51c6c7ee1264d862acd4`;
- exact manifest declares `ppmp.version: 2.0.0`.

Exact executable migration pressure:

- `conformance/fixtures/ppmp-v2/`;
- `conformance/ppmp_v2_migration_reference.py`;
- `conformance/test_ppmp_v2_migration.py`.

The fixture preserves material state, next actions, decisions, and checkpoint evidence, materializes a target Agnir Project, cold-starts it through current `repository-filesystem/0.1`, and explicitly rejects v1/RPM serialization as PPMP v2.

A second independently hosted historical PPMP v2 Project is not a release prerequisite; reproducible exact PPMP v2 conformance plus real non-fixture predecessor migration pressure is the durable release requirement.

## Conformance status

RC freeze head before this evidence record:

`5cb3503d058935e35a5af52b957cf72b2d5c2e14`

Agnir conformance run:

- run `33150361796` — success;
- workflow: `Agnir conformance`;
- self-hosting cold-start step — success;
- full `test_*.py` executable conformance suite — success.

An earlier full run including the first exact PPMP v2 migration fixture also passed in run `33150059494`, job `98779726021`.

## Known non-claim

Real mount-boundary behavior remains explicitly unproven until a mount-capable environment is available. No ordinary-directory simulation is accepted as substitute evidence. This does not block RC solely by absence of a fake test.

## Publication boundary

No public GitHub Release or release tag is created by this RC freeze. Repository preparation and `VERSION = 0.1.0-rc.1` do not themselves authorize public release publication.

Classification: **Agnir Core 0.1 compatibility frozen; repository line at 0.1.0-rc.1; RC conformance green; public stable release not yet published.**
