# v0.2.0 Stable Main Acceptance Verification — 2026-09-03

## Published stable receipts

- `v0.2.0` -> `fc84095ed5d500be9e1b43a4af0e93356571bbd4`;
- GitHub Release id `381710267`;
- publication/conformance run `33711982062` success;
- `releases/latest` -> `v0.2.0`;
- stable package baseline `f59a83754346982170142a355a01c94050ddf3a5`, run `33711830312` success.

## Reconciled main candidate

- captured main: `1af33e0cc470107aadaeb5d4d2f0f4570d81ee1d`;
- captured release source: `2eb083d2aaa2a0869b2baf9ba46d012913317102`;
- candidate: `08804f42262326db49fc573ca8fdf6b71b5e9734`;
- tree: `ccbe549100cc91bd1854950bae34cf4642192ea0`;
- candidate PR: #12;
- candidate-tree workflow: `33712370588` success.

PR #12's GitHub synthetic merge revision `8cf3733a77d125ccacd2bac0c8384a99394096ab` used tree `ccbe549100cc91bd1854950bae34cf4642192ea0`, exactly equal to the staged candidate tree. The PR was validation-only and was not used as the target-publication primitive.

Immediately before main advancement, fresh reads confirmed:

- `main` -> `1af33e0...`;
- `release/v0.2.0` -> `2eb083d2...`;
- `integration/v0.2.0-stable-main-reconcile` -> `08804f...`.

The candidate therefore remained fresh.

## Main publication

Main advanced exactly once from captured target `1af33e0...` directly to reconciled candidate `08804f...`. The two-parent candidate already contained the accepted stable Project/package result plus main-owned reconciled continuity, so no release-line-continuity-first window or follow-up repair commit was required.

Authoritative-main push workflow `33712492531` passed:

- Core `0.2` repository self-host cold start;
- stable Core/profile `0.1` compatibility regression;
- VCS branch continuity;
- non-VCS Core `0.2` parallel continuity;
- Core `0.2` VCS mapping;
- repository-filesystem `0.2` discovery;
- VCS lineage binding;
- semantic and concrete `0.1` → `0.2` migration;
- fresh Core `0.2` install and exact published-v0.1.1 migration/fresh resume;
- stable `v0.2.0` package gates;
- full conformance suite.

Both RC and stable publication jobs were skipped on the ordinary main push.

After direct main advancement, GitHub automatically marked validation PR #12 closed/merged by exact ancestry with `merge_commit_sha` equal to `08804f...`. This is ancestry recognition, not evidence that the PR merge API was used to publish target truth.

## Result

Authoritative main now accepts stable `v0.2.0` while preserving:

- Project `urn:agnir:project:agnir-core`;
- logical lineage `urn:agnir:lineage:authoritative`;
- selector `refs/heads/main`;
- Core/profile `0.2` stable contracts;
- Core/profile `0.1` compatibility/migration surfaces;
- immutable stable and RC tags.

The next phase is broader downstream Project/execution-surface evidence and explicit `V1_RELEASE_CRITERIA.md` gap closure, not another fundamental lineage redesign absent new evidence.
