# Agnir v1.0.1 activation-hardening authoritative acceptance

Date: 2026-09-09 (Project working timezone)

## Scope

This Evidence records target reconciliation and authoritative-main acceptance of the repository/distribution `1.0.1` activation/packaging hardening implemented through PR #50.

It does **not** record stable publication. Core remains `1.0`; `repository-filesystem` remains `repository-filesystem/1.0`; `v1.0.0` remains latest published stable until a separately authorized `v1.0.1` publication transaction succeeds.

## Staging acceptance

Candidate implementation evidence is recorded separately in:

`.agnir/evidence/2026-09-09-v1.0.1-activation-hardening-candidate.md`

Final checkpointed staging head:

- revision: `162b9069ed485834f3f2c6f68ab49e41200cfd4d`;
- PR: #50;
- final synthetic-merge conformance: run `34255122900` — success.

The staging continuity was treated as reconciliation input rather than copied as automatic target truth.

## Authoritative reconciliation

PR #50 was squash-merged into authoritative `main`:

- PR state: merged/closed;
- authoritative merge revision: `f07a792816956815876703472d55f711b5528387`;
- merge commit message: `release: implement v1.0.1 activation hardening`;
- authoritative lineage remains `urn:agnir:lineage:authoritative`;
- selector/binding remains `refs/heads/main`;
- Project identity remains `urn:agnir:project:agnir-core`.

Post-merge exact-source verification:

- workflow: `34255207555`;
- head branch: `main`;
- head revision: `f07a792816956815876703472d55f711b5528387`;
- conclusion: success;
- repository job: `102159143123` — success.

The repository job passed the self-host cold-start, historical 0.1/0.2 compatibility regression surfaces, Core/profile 1.0 stability/discovery/promotion surfaces, stable package gates, and the full conformance suite.

The `Publish v1.0.1 stable release` job was skipped, as required for an implementation merge that did not carry the publication-arm commit intent.

## Host-state readback

After authoritative merge and verification:

- repository branches: only `main`; completed `release/v1.0.1-activation-hardening` staging ref is retired;
- `releases/latest`: `v1.0.0`;
- release list contains no `v1.0.1` Release;
- tag refs contain no `v1.0.1` tag;
- immutable `v1.0.0` remains `6d16dcfd17b8e9f22fd25804e22b9f8a516d06c3`;
- immutable accepted `v1.0.0-rc.1` remains `092945289f1a0a9803e4fe0583104aa380ceaadc`.

Therefore repository source package `1.0.1` is implemented and accepted on authoritative `main`, while stable distribution resolution remains `v1.0.0`.

## Accepted operational result

The accepted `1.0.1` implementation establishes:

1. root `AGNIR.md` as canonical Executor-facing Project activation/operation instructions;
2. locator-only `AGENTS.md -> AGNIR.md` activation;
3. README activation heading retained only as a backward-compatible `1.0.0` locator;
4. legacy README route accepted only for safe pre-upgrade activation/repair;
5. repository-context short `commit` / `提交` / `提交代码` intent dispatches checkpoint evaluation before commit;
6. `commit and push` / `提交推送` adds push plus destination-ref verification;
7. checkpoint evaluation remains distinct from forced continuity mutation, so unchanged durable truth is still a no-op;
8. Project-defined pre-commit verification remains Project policy outside Agnir Core;
9. Skill install/upgrade/resume/repair procedure is reconciled to the direct `AGNIR.md` route;
10. publication remains a distinct exact-authoritative-main transaction.

## Next boundary

No further implementation reconciliation is required for v1.0.1 activation hardening.

The next release-specific boundary is stable publication, which requires separate explicit authorization. If authorized, the Project must re-resolve authoritative truth, use the exact main-only publication arm `release: publish v1.0.1 stable`, verify the resulting tag/Release/latest state and unchanged earlier immutable tags, then checkpoint publication receipts and update `agnir/operations` provenance.
