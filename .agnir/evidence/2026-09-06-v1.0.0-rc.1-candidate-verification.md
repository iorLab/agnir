# v1.0.0-rc.1 Candidate Verification — 2026-09-06

## Candidate

- release branch: `release/v1.0.0-rc.1`
- Project identity: `urn:agnir:project:agnir-core`
- logical lineage: `urn:agnir:lineage:v1.0.0-rc.1`
- selector: `refs/heads/release/v1.0.0-rc.1`
- repository version: `1.0.0-rc.1`
- Core/profile: `1.0` / `repository-filesystem/1.0`
- verified candidate revision: `e4d5ad8f7e4013314401ecf3d987edb66f509ce5`
- verified workflow run: `34026056687` — success

## Pre-green RC pressure

The RC cycle intentionally exercised the true 1.0 self-host path for the first time because the promotion PR source tree itself remained repository/Core/profile 0.2.

- candidate `6f680a885c0e61060ff9f06e2b1e7461586c16dc`, run `34025848630`: failed at `Repository self-host cold-start` because `check_agnir_1_0.py` required the brittle literal marker `coherent target publication` while the Core contract expressed the same accepted invariant as target reconciliation + coherent integration publication.
- candidate `8d06de59745c422753023836fe238f8bb5de6092`, run `34025962753`: failed at the same gate because the self-host checker required literal `logical Continuity Lineage identity` while the promotion contract consistently uses `logical lineage identity` / `continuity.lineage` terminology.
- neither failure exposed behavioral divergence in resolver, schema, checkpoint, lineage, migration, promotion, VCS, or compatibility semantics.
- Core 1.0 received an explicit non-behavioral terminology clarification for `coherent target publication`; the second checker marker was corrected to match the normative contract rather than forcing another grep-oriented wording change.

## First complete green RC-source run

Revision `e4d5ad8f7e4013314401ecf3d987edb66f509ce5` passed run `34026056687` with:

- repository Core/profile 1.0 self-host cold start — success;
- Core 0.1 compatibility regression — success;
- Core 0.2 non-VCS parallel continuity — success;
- Core 0.2 VCS mapping — success;
- repository-filesystem 0.2 discovery — success;
- Core 1.0 stability semantics — success;
- repository-filesystem 1.0 discovery — success;
- repository-filesystem 0.2→1.0 promotion — success;
- VCS lineage binding — success;
- Core 0.1→0.2 migration semantics — success;
- repository-filesystem 0.1→0.2 migration — success;
- fresh Core 0.2 install and exact published-v0.1.1 migration — success;
- full `test_*.py` suite — success;
- v0.2-only stable package gate — correctly skipped for the 1.0 RC source;
- v1 RC publication job — correctly skipped because the exact arm commit message was absent.

## Verdict

**RC candidate verification: PASS.**

Publication remains unarmed. This evidence must itself be checkpointed and the checkpoint source must pass exact release-branch conformance before the publication arm commit is created.
