# v1.0.0 stable staging acceptance

Date: 2026-09-06

## Verdict

**ACCEPTED AS STAGING INPUT.** The `release/v1.0.0` source package is suitable for target-side reconciliation into authoritative `main`. This evidence does not itself publish stable `v1.0.0` and does not make the release lineage authoritative.

## Exact accepted source

- branch: `release/v1.0.0`
- logical lineage: `urn:agnir:lineage:v1.0.0`
- accepted source revision: `65b5484b62bbd413d0984d5c952bc0a653da1964`
- source tree: `abe5715896c58a8f197700988ae03d4538a3b0d6`
- repository version: `1.0.0`
- Core: `1.0`
- profile: `repository-filesystem/1.0`

## Verification receipt

Workflow `34030660332`, repository job `101479440549`, completed successfully on exact revision `65b5484b62bbd413d0984d5c952bc0a653da1964`.

Successful gates included:

- Agnir Core/profile 1.0 self-host cold start;
- stable Core 0.1 compatibility regressions;
- Core 0.2 VCS and non-VCS continuity;
- repository-filesystem 0.2 discovery;
- Core 1.0 stability semantics;
- repository-filesystem 1.0 discovery;
- explicit repository-filesystem 0.2 -> 1.0 promotion;
- VCS lineage binding;
- Core/profile 0.1 -> 0.2 migration;
- fresh Core 0.2 install and exact published v0.1.1 migration;
- version-scoped stable package gates;
- complete `test_*.py` suite.

All publication jobs were skipped, as required for a staging branch.

## Prior candidate failure classification

Initial candidate `d2322518c9f6412bcf1ee159e233574b5c63c060`, run `34030546601`, passed every explicit stable/release gate but failed one full-suite assertion because `test_skill_package.py` expected lowercase `repository` while `VERSIONING.md` correctly used sentence-initial `Repository`. The repair changed only that test marker; no normative contract, resolver, stable package semantics, or publication behavior changed.

## Accepted RC dependency

This stable staging input remains downstream of the accepted immutable RC:

- `v1.0.0-rc.1` -> `092945289f1a0a9803e4fe0583104aa380ceaadc`;
- Release id `383536840`;
- workflow `34026167762` attempts 1 and 2 success;
- RC acceptance checkpoint `afc07d062b957e8dbfe3f859834c787e64aa52be`.

## Target-side boundary

The next step MUST re-read authoritative `main`. The stable product/package changes may be reconciled into the authoritative target, but release-lineage `AGNIR.yaml` and continuity are not copied wholesale. Authoritative Project identity, logical lineage identity, and selector/binding are preserved.

Stable publication remains main-only and dormant until an exact authoritative main candidate passes its own conformance and an exact main commit with message `release: publish v1.0.0 stable` arms publication.
