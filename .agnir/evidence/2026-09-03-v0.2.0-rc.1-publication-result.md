# v0.2.0-rc.1 publication result

Date: 2026-09-03

## Exact publication target

- tag: `v0.2.0-rc.1`
- tag target: `50a8cd565954e7e8055b8b628e2d620ac7357bab`
- object type: `commit`
- target tree: `fe0d8d19f5fbeb7cdd2a9ebd6b3417081cd78777`

The tag was created only after the complete conformance job succeeded on the exact target in GitHub Actions run `33675638723`.

## GitHub Release

- Release id: `381532232`
- title: `Agnir v0.2.0-rc.1`
- tag: `v0.2.0-rc.1`
- target commitish: `50a8cd565954e7e8055b8b628e2d620ac7357bab`
- `prerelease`: true
- `draft`: false
- published at: `2026-09-02T19:50:04Z`

Post-publication API reads independently confirmed the tag ref and Release metadata. GitHub `releases/latest` continued to resolve to stable `v0.1.1`, Release id `380414987`; RC publication therefore did not change latest-stable resolution.

## Workflow ordering

Run `33675638723` executed the complete release-branch conformance job first. RC self-host, stable Core `0.1` regression, VCS/lineage/profile/migration pressure, fresh Core `0.2` install, exact published-v0.1.1 migration fixture, and full suite all passed. Only after that job completed successfully did dependent job `Publish v0.2.0-rc.1 prerelease` receive `contents:write` and create/verify tag + Release.

The publication job is idempotent/fail-closed: an existing tag is accepted only if it already points to the exact workflow SHA, and the tag is never moved.

## Post-publication rule

This Evidence is written after publication and is therefore not part of the immutable tag target. That is intentional. The tag target is the exact pre-publication candidate proven by the workflow; later branch checkpoints may record newly knowable publication receipts but must never redefine or move the tag.
