# Agnir v1.0.1 stable publication

Date: 2026-09-09 (Project working timezone)

## Authorization

The Principal explicitly authorized stable publication with: `发布 v1.0.1`.

Publication was therefore treated as a separately authorized authoritative-main operation, not as an implication of implementation merge or green CI.

## Prepublication state

Immediately before the publication arm:

- authoritative `main`: `7613638b1d116a84481ee25ab0ae4de54f663f64`;
- repository source version: `1.0.1`;
- Core: `1.0`;
- profile: `repository-filesystem/1.0`;
- `releases/latest`: `v1.0.0`;
- no `v1.0.1` tag existed;
- `v1.0.0`: `6d16dcfd17b8e9f22fd25804e22b9f8a516d06c3`;
- `v1.0.0-rc.1`: `092945289f1a0a9803e4fe0583104aa380ceaadc`.

Checkpoint evaluation at the arm boundary was a no-op because publication outcome had not yet occurred and no durable Project truth needed artificial mutation before the arm.

## Publication arm

The exact publication-arm commit was created with the same source tree as the accepted implementation checkpoint and the exact required commit message:

`release: publish v1.0.1 stable`

Arm/tag revision:

`f56d25b22997c259c660651e7357334b063093e1`

This preserved the accepted `1.0.1` source tree while making publication intent explicit and machine-checkable.

## Publication workflow

Workflow run:

- run: `34301559338`;
- conclusion: **success**;
- repository/conformance job: `102309347343` — success;
- stable publication job: `102309386242` — success.

The repository job passed self-host cold start, historical Core/profile 0.1 and 0.2 regressions, Core/profile 1.0 stability/discovery/promotion, stable package gates, and the full conformance suite.

The publication job then passed exact-source verification and `Create immutable v1.0.1 tag and release`.

## Host-state verification

Accepted stable publication facts:

- tag: `v1.0.1`;
- tag revision: `f56d25b22997c259c660651e7357334b063093e1`;
- GitHub Release id: `385176185`;
- Release name: `Agnir v1.0.1`;
- draft: `false`;
- prerelease: `false`;
- published at: `2026-09-09T02:02:14Z`;
- `releases/latest == v1.0.1`;
- previous stable `v1.0.0` remains exactly `6d16dcfd17b8e9f22fd25804e22b9f8a516d06c3`;
- accepted RC `v1.0.0-rc.1` remains exactly `092945289f1a0a9803e4fe0583104aa380ceaadc`.

Core remains `1.0` and `repository-filesystem` remains `repository-filesystem/1.0`.

## Public Release-note metadata observation

The generated GitHub Release body has a cosmetic omission in one sentence: the intended literal `` `AGNIR.md` `` was consumed by shell command substitution in the unquoted heredoc, leaving the filename absent from that sentence. The tag, source revision, Release identity, draft/prerelease state, latest-stable resolution, and conformance results are unaffected.

This is a repository-host metadata/presentation defect, not a protocol or package-integrity defect. It must not be "repaired" by moving or recreating the published tag. A future authorized metadata repair may update only the Release body while preserving release identity.

## Accepted result

Agnir `v1.0.1` is now the latest published stable repository/distribution release.

The release is a PATCH-level activation/packaging hardening over `v1.0.0`; it does not change Core `1.0`, `repository-filesystem/1.0`, Project identity semantics, Continuity Lineage semantics, or checkpoint no-op semantics.
