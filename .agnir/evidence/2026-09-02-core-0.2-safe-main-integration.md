# Core 0.2 safe-main integration candidate — 2026-09-02

## Target and source

Authoritative target remains `main` at `1bbdb5b258645ec7c5e0109c9b17dbaac004e625` while this candidate is constructed and verified.

Integrated development source is `feature/core-0.2-lineage` at `68cc443d6c44929f1b71d9d534e9b0f73f9745bf`. That source is stacked on `feature/multibranch-continuity`, so one two-parent target revision can integrate PR `#4` and PR `#5` together.

Git compare reports the source 86 commits ahead of target, 0 behind, with 40 changed files. There is no target-side Project-content divergence to merge; the material reconciliation requirement is target continuity and release truth.

## Pre-integration verification

Agnir source checkpoint `68cc443d...` passed conformance run `33620080730`, including:

- stable Core `0.1` self-host cold-start;
- experimental VCS branch continuity;
- Core `0.2` non-VCS parallel continuity;
- Core `0.2` VCS mapping;
- repository-filesystem `0.2` discovery;
- VCS lineage binding;
- Core `0.1`→`0.2` semantic migration;
- concrete repository-filesystem `0.1`→`0.2` migration;
- full conformance suite.

The integrated source also contains completed Svif real-consumer evidence, whose final Svif checkpoint `d42489f72cc8985d353ccbf2f9b6ae7249fe6480` passed run `33619807614` 3/3.

## Target continuity reconciliation

The source branch's `.agnir/state.md`, `.agnir/next-actions.md`, and `.agnir/decisions.md` are not copied wholesale into authoritative `main`.

The reconciled target preserves still-valid main truth including:

- published stable `v0.1.1` and exact target `e9712357ab590e5c1e5357b3cf3219d07d789aff`;
- Core/profile `0.1` / `repository-filesystem/0.1` as the currently published compatibility line;
- Agnir name origin/product meaning;
- repository vs execution-surface activation separation;
- existing Project upgrade semantics and real `skills-hub` evidence;
- transactional checkpoint and commit/push semantics;
- README audience/information architecture;
- main-only long-lived branch governance.

It then adds the accepted pre-RC Core `0.2` continuity-lineage, selector/binding, coherent fork, reconciliation/publication, migration, Svif real-consumer, versioning, and release-candidate decisions.

Root `AGNIR.yaml` remains a Core/profile `0.1` self-host manifest with published operational release `0.1.1`; optional experiment metadata marks Core `0.2` as `real-consumer-validated` / `pre-rc`. This prevents main integration from masquerading as a published Core `0.2` migration or stable release.

## Candidate publication mechanism

The final integration commit is to have:

1. first parent: current authoritative `main`;
2. second parent: `feature/core-0.2-lineage` source checkpoint;
3. Project tree: the complete source Project result;
4. target-owned overrides: reconciled main `AGNIR.yaml`, Current State, Next Actions, Decisions, and this integration evidence.

That commit will first be exposed only through a temporary integration-candidate ref so CI can verify the exact tree while `main` remains unchanged.

If candidate CI is green and fresh target/source ref checks show no advancement since construction, `main` may advance directly to that exact two-parent commit. No ordinary GitHub merge and no follow-up continuity repair should be inserted between candidate verification and target publication.

## Release boundary after integration

Main integration is not itself `v0.2.0-rc.1`. After authoritative main verification, a separate RC change must deliberately promote intended Core/profile contract artifacts, update version/release metadata, bind the RC to an exact revision, and test fresh install plus explicit migration/resume from published `v0.1.1`.
