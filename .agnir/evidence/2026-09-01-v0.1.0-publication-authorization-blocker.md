# v0.1.0 publication authorization and tool-surface blocker — 2026-09-01

## Authorization

The Principal explicitly authorized formal publication of Agnir `v0.1.0`.

The intended release target is the verified publication candidate:

`2a0cb7bf2068b11f361e315670b2f2dc497b2588`

That revision passed exact-revision GitHub Actions `Agnir conformance` run `33463490510`, job `99718447961`.

## Preflight

The publication attempt verified through GitHub API reads that:

- Git ref `refs/tags/v0.1.0` did not exist;
- a GitHub Release for tag `v0.1.0` did not exist.

## Execution-surface blocker

The connected GitHub capability available in this execution context exposes repository reads, commits, branches/refs, and related repository mutations, but it does not expose a mutation for creating a Git tag or GitHub Release.

A branch named `v0.1.0` is not a valid substitute for the required Git tag and was not created.

No publication success is claimed.

## Durable handoff

Publication authorization is durable and does not need to be requested again. A later capable Executor should:

1. create Git tag `v0.1.0` pointing exactly to `2a0cb7bf2068b11f361e315670b2f2dc497b2588`;
2. create the GitHub Release from that tag;
3. verify both the tag target and release existence;
4. then update Agnir Current State / Next Actions to mark `0.1.0` published and enable `latest stable` upgrade for compatible existing Projects.
