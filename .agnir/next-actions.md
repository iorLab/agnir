# Agnir Next Actions

Agnir `v0.2.0` is stable and three materially different external Projects now provide direct Core/profile `0.2` evidence: Svif authoritative migration, FishUp real migration-line validation, and VocaPort authoritative fresh install. The minimum real-Project count and real upgrade-boundary gates are therefore satisfied. The remaining v1 work is no longer “find another repository”; it is evidence in genuinely different execution/storage/implementation dimensions.

1. **Obtain a second materially distinct Agnir execution surface/adapter.** Use DSH or another real Agent environment only if it actually opens an authorized Agnir Project and demonstrates fresh activation -> State/Next/Decisions/Evidence load -> Project work/checkpoint -> fresh resume without predecessor-private context. Do not count product runtimes, repository labels, or unrelated DSH plugins as surface evidence.
2. **Pressure the real mount boundary when a genuine mount-capable environment is available.** Exercise discovery and checkpoint behavior across the actual mount/visibility boundary; keep the limitation explicit until real evidence exists and do not replace it with a synthetic claim.
3. **Obtain independent-implementation documentation evidence.** Have an implementation or rigorous review reconstruct Core/profile behavior from published specs, migration docs, conformance, activation guidance, and repository maps without private design-chat context. Treat ambiguities as documentation/product defects.
4. **Refresh the formal v1 readiness snapshot after the next external evidence milestone.** The dated snapshot in `V1_RELEASE_CRITERIA.md` predates VocaPort; current canonical State records that the >=3 real-Project threshold is now satisfied.
5. **Keep FishUp production publication separate.** Its migration-line validation is complete. Do not advance FishUp `main` unless the Principal separately authorizes the Cloudflare/D1 production side effect caused by every main push.
6. **Prepare a `1.0.0-rc` cycle only after the second-surface, mount-boundary, and independent-implementation gates are credibly closed.** The exact RC candidate must rerun every normative suite from a fresh environment.
7. **Retire temporary release/validation/integration refs when a safe delete-ref path is available.** Preserve immutable commits, workflow runs, releases, and Evidence.
8. **Keep stable maintenance compatible.** Future `v0.2.x` fixes should preserve Core/profile `0.2` unless a deliberate new compatibility line is justified and explicitly migrated.

## Current high-value receipts

- stable Agnir `v0.2.0`: `fc84095ed5d500be9e1b43a4af0e93356571bbd4`, publication run `33711982062`;
- Svif authoritative published migration checkpoint: `eba1b8538c4692a08bf69452525b735d23564599`, run `33727957648`;
- FishUp validated migration-line head: `bea8c4e6e52347e1a0164596a5a9132b17de9631`, runs `33737783270` / `33737919224`;
- VocaPort authoritative installation candidate/publication: `37bc529f8c17af8deb1b0867932e4fa65f01d7e3`, candidate run `33786785234`, publication verifier `33787496205`;
- VocaPort post-install checkpoint: `eb9a3cca54d6e5daa80fbacc72624a735057328b`, fresh-resume verifier `33787760565`.

## Invariants

- Durable continuity belongs to the Project.
- Project identity != logical lineage identity != selector/revision receipt.
- Validation/source continuity is reconciliation input, not automatic target truth.
- Target publication is coherent and stale candidates fail.
- Published tags are immutable.
- Core/profile `0.1` -> `0.2` remains explicit migration; stable `0.2.x` maintenance does not silently redefine compatibility.
- Real evidence must come from real behavior; synthetic equivalence, repository affiliation, or product-platform diversity cannot substitute for the specific v1 gate being claimed.
