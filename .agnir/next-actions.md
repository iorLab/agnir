# Agnir Next Actions

Agnir `v0.2.0` is stable. The minimum real-Project, real upgrade-boundary, real parallel-lineage/reconciliation, VCS/non-VCS lineage, materially distinct execution-surface, and genuine mount-boundary evidence gates are satisfied. The independent-implementation gate remains open after a preserved clean challenge attempt returned `FAIL-IMPLEMENTATION` and also exposed public-contract drift that this checkpoint repairs.

1. **Verify the repaired authoritative source.** Run the complete Agnir conformance workflow after the documentation/conformance repair reaches `main`. Treat any failure as release-blocking and repair before another external challenge.
2. **Create a fresh independent-implementation rerun pinned to the repaired exact `main` revision.** The new reviewer/session must not inspect issue #14/#15 findings, prior challenge archives/reports, private Agnir design context, or `conformance/*_reference.py` before its Phase C boundary.
3. **Require a conforming independently authored resolver/checkpoint path.** The gate does not close merely because the public docs are understandable; the new Phase B implementation must itself conform, with auditable receipts for cold start/fresh resume, explicit failures, no sibling guessing, no-op/material/stale checkpoints, preservation, and lineage isolation.
4. **Treat any new ambiguity as a public product defect.** Do not coach the reviewer. Fix public specification/profile/schema/procedure/conformance material, pin a new source revision, and rerun from another genuinely fresh context when material clarification is required.
5. **Close the independent-implementation gate only on clean evidence.** A `PASS` requires no unresolved behavior-material public ambiguity and a conforming independent implementation. `FAIL-IMPLEMENTATION`, `FAIL-DOCS`, and `FAIL-CONFORMANCE` remain evidence but do not satisfy the gate.
6. **Only after independent acceptance, define the 1.0 promotion candidate.** The intended direction is to promote the proven stable semantics to a clear Core/profile `1.0` contract rather than ship repository `v1.0.0` with confusing pre-1.0 compatibility identifiers. Do not combine that promotion with this `0.2` documentation repair or retroactively rewrite `v0.2.0` history.
7. **Then prepare an explicit repository `1.0.0-rc` cycle.** Construct an exact RC candidate and rerun every normative suite from a fresh environment, including Core, repository/filesystem profile, migration/upgrade, failure paths, cold start/fresh resume, lineage, self-hosting, and release/package gates.
8. **Keep FishUp production publication separate.** Its migration-line validation is complete. Do not advance FishUp `main` unless the Principal separately authorizes the Cloudflare/D1 production side effect caused by every main push.
9. **Retire temporary release/validation/repair refs when a safe delete-ref path is available.** Preserve immutable commits, workflow runs, artifacts, issues, releases, and Evidence. Validation/repair lineages are evidence/staging inputs, not automatic authoritative Project continuity.
10. **Keep stable maintenance compatible.** Future `v0.2.x` fixes may repair documentation, conformance, packaging, or implementation but must preserve Core/profile `0.2` semantics unless a deliberate new compatibility line is justified and explicitly migrated.

## Current high-value receipts

- stable Agnir `v0.2.0`: `fc84095ed5d500be9e1b43a4af0e93356571bbd4`, publication run `33711982062`;
- Svif authoritative published migration checkpoint: `eba1b8538c4692a08bf69452525b735d23564599`, run `33727957648`;
- FishUp validated migration-line head: `bea8c4e6e52347e1a0164596a5a9132b17de9631`, runs `33737783270` / `33737919224`;
- VocaPort authoritative installation candidate/publication: `37bc529f8c17af8deb1b0867932e4fa65f01d7e3`, candidate run `33786785234`, publication verifier `33787496205`;
- VocaPort post-install checkpoint: `eb9a3cca54d6e5daa80fbacc72624a735057328b`, fresh-resume verifier `33787760565`;
- DSH Session 1 / corrected / Session 2 checkpoints: `b4f87d3ebd86d647adc2b7b101498ca4c80e6287` / `29549ebf45071003ae3e885664c7c9e960d838eb` / `af9b9c0b725ae40d11e462f11e3a9392afed6d8a`;
- genuine mount-boundary final validation head: `ce30bed039e1cd2d1d8cf27b3cd2492c6dd279fa`, accepted run/job `33860631526` / `100984005488`;
- mount-boundary artifact: id `9931961351`, digest `sha256:2c7bb33c87e4de0e95542cfb12b3759ecdb005c6085962856bb4a2ad052b25ce`;
- independent challenge issue #15 pinned source: `d4d5c5a441766ca5993366429ecf6235d7c2a7bc`;
- preserved challenge archive SHA-256: `1426e0c4a3b9030944ad2694aaf9ff7daf4690b3f7fb1ce8cab9ba3f1dcc4a61`;
- challenge #15 verdict: `FAIL-IMPLEMENTATION`; gate remains open.

## Invariants

- Durable continuity belongs to the Project.
- Project identity != logical lineage identity != selector/revision receipt.
- Validation/source continuity is reconciliation input, not automatic target truth.
- Target publication is coherent and stale candidates fail.
- Published tags are immutable.
- Core/profile `0.1` -> `0.2` remains explicit migration; stable `0.2.x` maintenance does not silently redefine compatibility.
- A fresh independent reviewer must not receive private answers or prior-reference knowledge before the challenge's allowed comparison boundary.
- Real evidence must come from real behavior; synthetic equivalence, repository affiliation, product-platform diversity, ordinary filesystem behavior, or private reviewer coaching cannot substitute for the specific v1 gate being claimed.
